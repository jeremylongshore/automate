{
  "name": "automate",
  "version": "1.0.2",
  "type": "module",
  "private": true,
  "description": "Connect to Kobiton mobile testing platform - manage devices, run automation suites, and view test results",
  "scripts": {
    "build": "node scripts/build-tool-definitions.js",
    "validate": "node scripts/validate.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "ejs": "^5.0.1"
  },
  "pnpm": {
    "overrides": {
      "picomatch": ">=4.0.4",
      "vite": ">=7.3.2"
    }
  }
}
